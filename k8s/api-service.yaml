apiVersion: v1
kind: Service
metadata:
  name: rag-testing-api
  namespace: rag-testing
  labels:
    app: rag-testing-api
spec:
  type: LoadBalancer  # Use ClusterIP if behind Ingress
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: rag-testing-api
